<%= form_with model: @genre,url: admin_genre_path(@genre), local:true do |f| %>
  <%= f.label :"ジャンル名" %>
  <%= f.text_field :name, placeholder: "ジャンル名", class: "mx-3" %>
  <%= f.submit submit, class: "btn btn-info btn-sm my-2" %>
<% end %>